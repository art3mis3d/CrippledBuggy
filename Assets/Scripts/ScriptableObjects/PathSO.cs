using UnityEngine;

/// <summary>
/// Paths are used to determine the connecting entrances and exits between Locations.
/// They don't have a direction, so the same path SO can be reused in both ways, by referncing it in the appropriate <c>LocationEntrance</c> scripts in the scene.
/// </summary>
public class PathSO : MonoBehaviour { }